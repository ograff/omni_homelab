apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: frigate
  labels:
    app.kubernetes.io/name: frigate
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: frigate
  endpoints:
    - port: http
      interval: 30s
      path: /api/metrics
